---
title: "Modelos de Comunicação"
description: "Entenda como funcionam os dois modelos de comunicação utilizados nas APIs da DataBox360: REST e WebSocket."
---

## Entenda como funcionam

Com as APIs da **DataBox360**, você pode criar projetos integrados com diversas camadas de origem, incorporando dados em seu site, aplicativo ou sistemas internos.

As APIs são desenvolvidas com base em dois modelos de comunicação:

### 🌐 REST (Representational State Transfer)

A arquitetura REST utiliza o protocolo HTTP e é amplamente usada para construir aplicações Web. Esse modelo permite que interfaces se comuniquem por meio de requisições simples, geralmente retornando dados nos formatos:

- `JSON`
- `XML`
- `Texto puro`

Esse é o modelo mais comum e recomendado para a maioria das integrações com os serviços da DataBox360.

### 🔄 WebSocket

WebSocket é uma tecnologia avançada que permite abrir uma **sessão de comunicação interativa contínua** entre o navegador do usuário (ou aplicação cliente) e o servidor.

Esse modelo é ideal para aplicações que exigem:

- Atualizações em tempo real
- Menor latência
- Comunicação bidirecional contínua

## 🔒 Autenticação obrigatória

Para acessar os recursos oferecidos pelas APIs da DataBox360, é necessário realizar **autenticação prévia**. Cada módulo possui um fluxo de autenticação específico, garantindo segurança e rastreabilidade.

## 🧭 Arquitetura das APIs

A seguir, uma ilustração conceitual do modelo REST utilizado:

![Modelo REST](/rest.png)